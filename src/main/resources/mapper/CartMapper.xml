<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.faitmain.domain.product.mapper.CartMapper">

    <!-- 카트 추가 -->
    <insert id="addCart" parameterType="com.faitmain.domain.product.domain.Cart">
        INSERT
            INTO cart( user_id
                     , product_order_count
                     , product_number
                     )
        VALUES ( #{userId}
               , #{productOrderCount}
               , #{cartProduct.productNumber}
               )
    </insert>

    <!-- 카트 제거 (주문) -->
    <delete id="deleteCart">
        DELETE
          FROM cart
         WHERE cart_number = #{cartNumber}
    </delete>

    <!-- 카트 확인 -->
    <select id="getCart" parameterType="com.faitmain.domain.product.domain.Cart">
        SELECT *
          FROM cart
         WHERE user_id = #{userId}
           AND product_number = #{cartProduct.productNumber};
    </select>

    <!-- 카트 목록 조회 -->
    <select id="getCartList" parameterType="com.faitmain.domain.product.domain.Cart">
        SELECT c.cart_number, c.user_id, c.product_number, p.product_name, p.product_price
          FROM cart                    c
               LEFT OUTER JOIN product p ON p.product_number = c.product_number
         WHERE user_id = #{userId}
    </select>

    <!-- 장바구니 수정 -->
    <update id="updateCart" parameterType="com.faitmain.domain.product.domain.Cart">
        UPDATE cart
        <set>
            product_order_count = #{productOrderCount}
        </set>
        WHERE cart_number = #{cartNumber}
    </update>


</mapper>